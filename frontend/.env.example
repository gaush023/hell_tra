# ================================
# API Configuration
# ================================

# Backend API Base URL
# If not set, will auto-detect based on window.location.protocol
# Examples:
# - Development HTTP: http://localhost:3001
# - Development HTTPS: https://localhost:3001
# - Production: https://api.yourdomain.com
# VITE_API_BASE_URL=https://localhost:3001

# WebSocket URL
# If not set, will auto-detect based on window.location.protocol
# Examples:
# - Development HTTP: ws://localhost:3001/ws
# - Development HTTPS: wss://localhost:3001/ws
# - Production: wss://api.yourdomain.com/ws
# VITE_WS_URL=wss://localhost:3001/ws

# ================================
# Notes
# ================================
#
# By default, the frontend will auto-detect the protocol (HTTP/HTTPS) and
# construct the API and WebSocket URLs automatically. Only set these variables
# if you need to override the default behavior.
#
# Auto-detection logic:
# - API: {window.location.protocol}//localhost:3001
# - WebSocket: {wss: if https, ws: if http}//localhost:3001/ws
